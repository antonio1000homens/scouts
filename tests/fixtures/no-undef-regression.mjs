// This fixture is intentionally invalid. The hermetic runner must reject it;
// keeping the check here prevents the no-undef configuration from becoming a
// silent no-op.
export function brokenHandler(messageBody) {
  return message;
}
